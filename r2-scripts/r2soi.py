"""
Find strings of interest in a target file from a default list or
from an input file of search strings using r2.
"""
import r2pipe
import os

def r2soi(target, soi):
    """
    Find strings of interest in target file
    """
    if 
    r2p = r2pipe.open(target)
    if r2p.cmd('iqz')
